<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="RentalApp.Views.ItemDetailPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:RentalApp.ViewModels"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             x:DataType="vm:ItemDetailViewModel"
             Title="{Binding Title}">

    <ScrollView>
        <StackLayout Padding="20" Spacing="16">

            <ActivityIndicator IsRunning="{Binding IsBusy}"
                               IsVisible="{Binding IsBusy}"
                               Color="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                               HorizontalOptions="Center" />

            <!-- Error -->
            <Border BackgroundColor="{AppThemeBinding Light=#FFEBEE, Dark=#1B0000}"
                    Stroke="{AppThemeBinding Light=#F44336, Dark=#EF5350}"
                    StrokeThickness="1" Padding="15"
                    IsVisible="{Binding HasError}">
                <Border.StrokeShape><RoundRectangle CornerRadius="8" /></Border.StrokeShape>
                <Label Text="{Binding ErrorMessage}"
                       TextColor="{AppThemeBinding Light=#D32F2F, Dark=#EF5350}" FontSize="14" />
            </Border>

            <!-- Item info -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}"
                   Padding="20" CornerRadius="12" HasShadow="True"
                   IsVisible="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}">
                <StackLayout Spacing="10">
                    <Label Text="{Binding Item.Title}" FontSize="22" FontAttributes="Bold" />
                    <Label Text="{Binding Item.Category.Name}"
                           FontSize="13"
                           TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}" />
                    <Label Text="{Binding Item.DailyRate, StringFormat='£{0:F2} per day'}"
                           FontSize="20" FontAttributes="Bold"
                           TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}" />
                    <BoxView HeightRequest="1"
                             BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}" />
                    <Label Text="{Binding Item.Description}" FontSize="15" />
                    <Label Text="{Binding Item.Owner.FullName, StringFormat='Listed by {0}'}"
                           FontSize="13"
                           TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray500}}" />
                    <Label Text="{Binding AverageRating}"
                           FontSize="14"
                           TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                </StackLayout>
            </Frame>

            <!-- Rental request form (hidden for owner) -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray900}}"
                   Padding="20" CornerRadius="12" HasShadow="True"
                   IsVisible="{Binding IsOwner, Converter={StaticResource InvertedBoolConverter}}">
                <StackLayout Spacing="12">
                    <Label Text="Request a Rental" FontSize="18" FontAttributes="Bold" />

                    <Label Text="Start Date" FontSize="14" FontAttributes="Bold" />
                    <DatePicker Date="{Binding StartDate}"
                                MinimumDate="{x:Static sys:DateTime.Today}"
                                FontSize="15" />

                    <Label Text="End Date" FontSize="14" FontAttributes="Bold" />
                    <DatePicker Date="{Binding EndDate}"
                                MinimumDate="{x:Static sys:DateTime.Today}"
                                FontSize="15" />

                    <Label Text="{Binding EstimatedTotal, StringFormat='Total: {0}'}"
                           FontSize="16" FontAttributes="Bold"
                           IsVisible="{Binding EstimatedTotal, Converter={StaticResource StringToBoolConverter}}"
                           TextColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}" />

                    <Button Text="Request Rental"
                            Command="{Binding RequestRentalCommand}"
                            BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                            TextColor="White"
                            FontSize="16" FontAttributes="Bold"
                            CornerRadius="8" HeightRequest="50" />
                </StackLayout>
            </Frame>

            <!-- Owner controls -->
            <StackLayout IsVisible="{Binding IsOwner}" Spacing="10">
                <Label Text="This is your listing."
                       HorizontalOptions="Center"
                       TextColor="{AppThemeBinding Light={StaticResource Gray500}, Dark={StaticResource Gray500}}"
                       FontSize="14" />
                <Button Text="Edit Listing"
                        Command="{Binding NavigateToEditCommand}"
                        BackgroundColor="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                        TextColor="White"
                        CornerRadius="8"
                        HeightRequest="50" />
            </StackLayout>
        </StackLayout>
    </ScrollView>
</ContentPage>
